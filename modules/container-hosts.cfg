define host{
        use 				server-noping
		host_name 			nginx-container
		alias      			nginx containers
		hostgroups   		nginx
		}

define hostgroup{
		hostgroup_name   	nginx      ; The name of the hostgroup
		alias      			Nginx service   ; Long name of the group
		}
